# No target because scopable.
# No value because same as plugin ID.
dark:
  label: "Dark"
  key: "data-bs-theme"

# No target because scopable.
# No value because same as plugin ID.
light:
  label: "Light"
  key: "data-bs-theme"
